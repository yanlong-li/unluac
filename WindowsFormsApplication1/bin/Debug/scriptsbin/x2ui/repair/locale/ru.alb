if X2Util:GetGameProvider() == MAILRU then
  repairLocale.width = {partDesc = 150}
  repairLocale.anchor = {
    partRepairButtonX = 20,
    partRepairWindowX = 10,
    allRepairCostX = 20,
    leftMoneyY = 0
  }
end
