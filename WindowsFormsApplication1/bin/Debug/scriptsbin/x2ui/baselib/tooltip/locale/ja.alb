if X2Util:GetGameProvider() == GAMEON then
  tooltipLocale = {
    forclyUseLeftSection = true,
    uccFrontIcon = {
      anchor = "BOTTOM",
      offset = {0, 1}
    },
    skillSynergySection = {startOffsetY = 28},
    GradeString = function(evolveStr, grade)
      return string.format("%s(~%s)", evolveStr, grade)
    end
  }
  FORM_WIDGET_TOOPTIP = {SOCKET_ANCHOR_OFFSET = 3}
end
