if X2Util:GetGameProvider() == TRION and X2Locale:GetLocale() == "fr" then
  ingameshopLocale.mainTabFontSize = FONT_SIZE.MIDDLE
  ingameshopLocale.maxItemName = {
    list = 22,
    cart = 22,
    result = 33,
    confirmTitle = 41,
    confirmDetail = 20,
    confirmPackage = 20
  }
  ingameshopLocale.eventIconFixedExtent = true
end
