if X2Util:GetGameProvider() == TRION and X2Locale:GetLocale() == "fr" then
  territoryLocale.width = 600
  territoryLocale.territoryTab = {
    listCtrlFontSize = FONT_SIZE.SMALL,
    listCtrlFontAlign = ALIGN_LEFT
  }
end
